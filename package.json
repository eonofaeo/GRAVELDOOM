{
  "name": "gravebloom",
  "version": "0.1.0",
  "description": "GRAVEBLOOM — A Requiem in Ash and Crimson",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "typecheck": "npx tsc --noEmit",
    "test": "npx tsc --outDir .test-dist --module NodeNext --moduleResolution NodeNext --target ES2022 --skipLibCheck src/data/gameData.ts src/systems/cindersmithing.ts src/systems/saveSystem.ts && node --test tests/*.mjs",
    "preview": "npx vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
