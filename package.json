{
  "name": "gravebloom",
  "version": "0.1.0",
  "description": "GRAVEBLOOM — A Requiem in Ash and Crimson",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "typecheck": "npx tsc --noEmit",
    "preview": "npx vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
